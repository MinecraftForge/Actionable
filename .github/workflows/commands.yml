on: issue_comment
        
permissions:
  contents: read
  issues: write
  pull-requests: write
  discussions: write
 
name: Slash commands
jobs:
  check_comments:
    name: Check comments for /assign
    runs-on: ubuntu-latest
    steps:
      - uses: navikt/github-app-token-generator@v1
        id: generate_token
        with:
          private-key: ${{ secrets.GH_APP_KEY }}
          app-id: ${{ secrets.GH_APP_NAME }}
      - uses: MinecraftForge/Actionable@faa9b2e692547b50655e9267756fda670728ba23
        with:
          prefixes: "@forgebot<ws>,/@autoforge<ws>"
          github-token: ${{ steps.generate_token.outputs.token }}
          triage-team: triage